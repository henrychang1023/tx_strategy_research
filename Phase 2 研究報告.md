# Phase 2 研究報告：台指期無縫轉倉連續契約(Continuous Contract)

## 範圍

資料期間 2020-01-02 ~ 2025-12-31，共 1459 個交易日、72 個 TX 契約月份。輸入為 Phase 1
清洗後的 `data/clean/taifex_tx.csv`（僅 Regular session）。程式碼見
`internal/contract`、`internal/roll`、`cmd/continuous`；設計文件見
`C:\Users\User\.claude\plans\bright-watching-cerf.md`。

## 方法論

### 轉倉規則

實作三種前月(front)選取策略，共用同一套 back-adjust 引擎：

| 方法 | 規則 |
|---|---|
| Fixed | 固定於結算日前一個交易日轉倉 |
| Volume | 次月 Volume 超過前月時轉倉（一旦轉倉不會轉回），並以 Fixed 規則的轉倉日作為強制上限 |
| OI | 同 Volume，改比較 Open Interest |

三者都不會用到契約自身結算日當天的資料——強制轉倉上限訂在結算日前一個交易日，
確保 Phase 1 已知「結算日當天 settlement_price 不可信任」的那一列永遠不會被選為
front，也不會被用來計算轉倉價差。

### Back-adjustment

採用標準的 backward（Panama）累積價差法：最新一段契約維持原始報價不變，
每次轉倉往回推的舊契約整段，依「轉倉當天：舊契約收盤 − 新契約收盤」的價差整段平移，
Open/High/Low 套用同一個位移量，維持 OHLC 內部關係不變。Volume / OpenInterest
不調整。細節與正確性已用單元測試鎖定（`internal/roll/build_test.go`），包含：
- 轉倉當天前後的調整後價格變動 = 舊契約自己的真實單日變動（不是跨契約價差）
- 最新一天 Adjusted == Raw

## 特例：結算日例外

台灣證交所因農曆春節連續休市，理論結算日 2023-01-18 當週沒有交易，實際結算日
順延到 **2023-01-30**，比公式晚了 12 天。這與原始資料中「結算日當天 settlement_price
= 0」的已知現象完全對應——2023-01-30 那筆 202301 資料的 settlement_price 正是 0。

修正方式不需要 `expiry.csv`：直接用原始資料自己的訊號——掃描每個契約月份，若存在
`settlement_price == 0` 的 Regular-session 列，該列日期即為真實結算日；找不到則
（該契約在資料範圍內尚未到期）才退回理論公式，僅影響排序、不影響任何轉倉邏輯。

## 結果統計

- 三種方法都產生 72 次轉倉（等於契約月份數，符合預期：每個月都會被轉倉一次）
- **Volume 方法與 Fixed 完全相同**（72 次轉倉日期無一天不同）：TX 前月的成交量在
  資料期間內從未在強制轉倉點之前就被次月超過，顯示台指期前月流動性在到期前一直
  維持主導地位
- **OI 方法與 Fixed 有 4 次相差 1 個交易日**（提早轉倉），其餘 68 次相同：

  | 提早轉倉日 | 契約 | Fixed 轉倉日 |
  |---|---|---|
  | 2020-05-18 | 202006 | 2020-05-19 |
  | 2023-12-18 | 202401 | 2023-12-19 |
  | 2025-05-19 | 202506 | 2025-05-20 |
  | 2025-06-16 | 202507 | 2025-06-17 |

  提早一天且方向一致（未平倉部位比成交量更早移轉到次月），符合市場微觀結構的
  一般直覺，可作為 OI 選取邏輯運作正常的旁證。

- 累積 back-adjust 幅度：以 2020-01-02（資料最早一天）為例，raw close 12101 點，
  Fixed/Volume 調整後為 8475 點（累積位移 -3626 點），OI 為 8496 點（-3605 點）——
  6 年 72 次轉倉平均每次約 50 點價差，量級與逐次轉倉印出的價差（十幾點到一百多點）
  一致，沒有異常放大或縮小。

## 驗證方式

1. `go build/vet/test ./...` 全過，含 `internal/contract`、`internal/roll` 的合成
   案例測試（轉倉判斷、強制轉倉、back-adjust 數學）
2. 實際資料跑 `go run ./cmd/continuous`：72 次轉倉全數成功，無資料缺漏或例外錯誤
3. 人工抽查 2023-01-17 轉倉點（`data/clean/continuous/fixed.csv`）：raw close 出現
   -28 點跳空，adjusted close 只有 -13 點（等於 202301 自己在同一天的真實變動），
   確認沒有使用到 2023-01-30 那個不可信的結算價
4. 抽查最新一天（2025-12-31）：Adjusted 完全等於 Raw，符合 backward 慣例

## 下一步

Phase 3 回測引擎：手續費/交易稅/滑價、雙軌報酬會計（純點數 vs 模擬保證金資金報酬）、
PnL / Sharpe / Max Drawdown。三個轉倉方法的連續契約（`data/clean/continuous/*.csv`）
都已就緒，可直接作為回測輸入。
