# 台指期無縫轉倉研究計畫

## 目標

建立 Go 撰寫的研究平台，完成：
- 台指期連續契約(Continuous Contract)無縫轉倉回測
- 與台股加權指數比較
- Basis 分析
- 完成基礎研究後，將轉倉日、進場、停損、成本、槓桿等維度參數化，比較不同策略組合

## 現有資料盤點

目前 `strategy/` 下已有三份原始資料：

```text
future_historic_data/   台交所全商品期貨日資料 2020~2025（逐年 CSV）
TAIEX_historic_data/    台股加權指數日資料（Price Index / Total Return Index）
date/expiry.csv         台指期結算日對照表
```

### 資料驗證結果與注意事項

1.  **`future_historic_data` 是全商品原始資料，非純 TX**
    -   同一天同一契約會有 `Regular`（日盤）與 `After-Hours`（夜盤）兩列，`settlement_price` /
        `open_interest` 只有 `Regular` 列有值
    -   `contract month` 欄位混有單一契約列（如 `202001`）與跨月價差列（如
        `202001/202002`），價差列必須過濾掉
    -   為了跟只在日盤交易的加權指數比較，Continuous Contract 以 `Regular` session 為主
    -   **各年度檔案格式不一致**：2020 年日期格式為 `2020/1/2`（不補零），2021~2025
        年為 `2021/01/04`（補零）；contract month 欄位的空白數量也不同。Parser
        必須用容錯的日期解析與欄位 trim，不能假設全部年度格式相同
    -   **結算日當天 `settlement_price` 欄位為 `0`**（已跨年度抽樣驗證），不可信任，
        當天的最終結算價需改用 `date/expiry.csv`
2.  **`date/expiry.csv`**
    -   332 列中，77 列為月契約（`contract delivery month` 無 `W` 後綴），254
        列為週契約（`W1`~`W5`）。原始 TX 期貨資料中完全沒有週契約，
        週契約屬於另一商品，過濾時只取無 `W` 後綴的列
    -   已抽樣跨年度驗證：月契約列的日期與結算價格與原始資料吻合
    -   檔案編碼為 ISO-8859（非 UTF-8），讀取時需指定編碼，避免中文欄位亂碼
    -   台指期結算規則固定：交割月份第三個星期三為最後交易日。可改用程式計算
        （每月第三個星期三），並用本檔案交叉驗證；驗證通過後，往後不需要持續維護
        這份 CSV，只作為歷史驗證快照保留
3.  **`TAIEX_historic_data`**
    -   資料起始於 `2021/1/4`，但期貨原始資料從 `2020/1/2`
        即有，兩者有約一年的落差。2020 年這段期間僅能做期貨面分析，
        無法做加權指數比較，需在報表中如實標註此限制
    -   同時提供 Price Index 與 Total Return Index，後者含股利再投入，
        適合用來跟「無縫轉倉」的累積報酬做對照（單純點數報酬較適合對照
        Price Index）

## 專案階段

### Phase 1 資料收集與清洗

目標：把現有原始資料轉成可重現、乾淨的研究用資料集。

#### 所需資料（現況）

1.  台指期每日資料 — 已有（`future_historic_data`，需清洗，見上）
2.  台股加權指數每日資料 — 已有（`TAIEX_historic_data`，2021 年後）
3.  每月結算日 — 已有（`date/expiry.csv`，改為程式計算 + 交叉驗證）
4.  契約月份資訊 — 由 1. 的 contract month 欄位取得

#### 建議流程

1.  Parser：讀取三份原始資料，統一轉成 Go struct。
    -   期貨資料：filter `contract == "TX"`、`Trading Session == "Regular"`、
        排除跨月價差列，日期用容錯解析
    -   結算日：以「每月第三個星期三」規則產生，用 `expiry.csv` 月契約列交叉驗證
2.  保留 raw 資料，不直接修改。
3.  清洗後輸出 clean 資料（統一日期格式、數值型別、排序）。
4.  MVP 先以現有 CSV 為主；之後有需要再實作 Downloader 自動補資料，
    避免一開始投入瀏覽器爬蟲。

#### 建議目錄

``` text
data/
  raw/
    taifex/
    twse/
    expiry/
  clean/
  cache/
```

### Phase 2 Continuous Contract

實作：
- 固定結算日前一天轉倉
- Volume 切換
- Open Interest 切換
- Back-adjust 修正價格跳空（轉倉當天以 `expiry.csv` 結算價為準，而非 fut csv 當天的 settlement_price）

### Phase 3 回測引擎

加入：
- 手續費、交易稅、滑價
- 雙軌報酬會計模式：
  1.  純點數報酬（不考慮保證金，方便跟大盤點數報酬直接比較）
  2.  模擬實際保證金部位的資金報酬（含槓桿倍數、保證金追繳邏輯）
- PnL、Sharpe、Max Drawdown（兩種會計模式都要能輸出，方便比較）

### Phase 4 Basis 分析

建立欄位：
- Future、Index、Basis、DaysToExpiry

分析：
- Basis 收斂
- Rolling Cost
- Contango / Backwardation

### Phase 5 策略參數化研究

在 Phase 2~4 的基礎完成後，把以下維度改為可替換參數，方便組合比較：

-   轉倉日：最後交易日、前一天、OI 切換日
-   進場策略
-   停損規則
-   成本假設（手續費、稅、滑價高低情境）
-   槓桿倍數

## Go 專案架構

``` text
cmd/
internal/
  datasource/
  parser/
  cleaner/
  loader/
  contract/
  roll/
  strategy/
  engine/
  report/
data/
output/
```

### datasource

負責下載資料（未來可替換來源）。

### parser

CSV -> Struct，含期貨、指數、結算日三種來源，並處理各年度格式差異。

### cleaner

執行 Phase 1 列出的過濾/清洗規則（session、價差列、日期格式、結算日交叉驗證）。

### roll

建立 Continuous Contract，轉倉當天價格以 `expiry.csv` 結算價為準。

### engine

計算績效，同時支援純點數與保證金資金兩種會計模式。

### strategy

交易策略，Phase 5 起支援參數化組合。

## 注意事項

-   Raw 永遠保留，不覆寫。
-   Parser 與回測引擎解耦。
-   每個步驟都可重新產生。
-   MVP 先完成研究，再做自動化。
-   不建議一開始使用 Selenium/Playwright，優先 HTTP Downloader。
-   各年度原始檔案格式可能不一致（日期補零、欄位空白），清洗層要能容錯，
    不能假設所有檔案格式相同。

## 建議里程碑

1.  一週：資料清洗（含格式差異處理）、Parser、結算日驗證。
2.  二週：Continuous Contract、回測引擎（雙軌會計模式）。
3.  二週：Basis 分析、績效報表、策略比較。
4.  後續：Phase 5 策略參數化研究。

## 後續可擴充

-   分鐘 K
-   多商品（MTX 小型台指、TMF 微型台指，資料中已存在）
-   Web API
-   視覺化 Dashboard
-   自動每日更新
