# 模擬策略：風險相近下的報酬率比較

## 目標

固定用 Phase 2「結算日前一天轉倉」（fixed 轉倉法）的連續契約，調整資金模式的槓桿
倍率，讓 TX 轉倉策略的波動風險跟「直接投資台股加權指數」相近，再比較兩者的報酬率
——而不是像 Phase 3/4 報告那樣，直接比較一個高槓桿（追繳頻繁、最大回撤動輒
400%~700%）的期貨部位跟一個完全不用槓桿的現貨指數，那不是公平的比較。程式碼見
`cmd/simulate`。比較區間沿用 Phase 4 已確認過的 TX/TAIEX 完全對齊區間：
2021-01-04 ~ 2025-12-31（1214 個交易日）。

「直接投資台股加權指數」用 TAIEX Total Return Index（含股利再投入）代表，因為
真的買 ETF 或指數型商品會領到股利、股利也會再投入，比只看 Price Index 更貼近
真實的「指數投資」結果。

## 方法論：怎麼校準槓桿

**波動風險**用逐日報酬的標準差衡量。TX 策略在固定 1 口部位、不隨權益調整倉位的
設定下（見 Phase 3 說明），逐日台幣損益 `dayPnL` 完全不受本金大小影響——本金只
決定 `dayPnL/本金` 這個報酬率的分母。這代表不需要用搜尋或試誤法去找槓桿倍率，
可以直接解方程式：

```
TX 策略年化波動度 = stdev(dayPnL) / 本金 × √252
目標：TX 策略年化波動度 = TAIEX 年化波動度
=> 本金 = stdev(dayPnL) / stdev(TAIEX 逐日報酬率)
```

程式裡用 `engine.RunFundMode` 先跑一次本金隨便設 1 元的版本，取出跟本金無關的
`NetPnL`（`internal/engine/fund.go` 已經證明過追繳注資不會污染這個數字，見 Phase 3
報告），算出 `stdev(dayPnL)`，再用上面的公式直接解出本金，不需要反覆嘗試。

## 校準結果

| 項目 | 數值 |
|---|---|
| TX 策略每日台幣損益標準差 | NT$49,584 |
| TAIEX 累積報酬指數每日報酬標準差 | 1.2228% |
| **風險相近所需本金** | **NT$4,054,925** |
| 2021-01-04 起始名目價值（1 口 TX，14873 點 × 200） | NT$2,974,600 |
| 隱含起始槓桿倍數 | 約 0.73x |

驗證：用這個本金重新跑一次完整資金模式模擬，兩邊年化波動度都是 **19.41%**，
校準無誤。

值得注意的是隱含槓桿倍數是 **0.73x，小於 1**——也就是說，要讓 TX 期貨部位的
波動風險降到跟現貨指數一樣，本金反而要超過部位的名目價值，比「完全不用槓桿、
現金全額對應部位名目價值」還保守。這說明 TX 期貨本身逐日的點數波動略高於台股加權
指數本身的逐日報酬波動，屬於合理現象（期貨市場的價格發現/流動性动態，通常會讓
期貨的日內或日間波動略高於對應的現貨指數）。

## 風險相近後的報酬率比較

| 標的 | 總報酬% | Sharpe | 最大回撤% | 追繳次數 |
|---|---|---|---|---|
| TX 轉倉策略（校準槓桿 0.73x，medium 成本） | +81.6% | 0.87 | 36.9% | 0 |
| TAIEX Total Return Index | +129.1% | 0.98 | 51.4% | 0（無槓桿，不適用） |

**風險相近的前提下，TX 轉倉策略的報酬率（+81.6%）明顯落後台股加權指數直接投資
（+129.1%）**，Sharpe 也略低（0.87 vs 0.98）。這跟 Phase 3/4 報告「TX 報酬率超越
TAIEX Total Return Index」的結論方向相反，原因很清楚：Phase 3/4 比較的是一個
本金只有 NT$368,000（2x 保證金）的高槓桿部位（隱含槓桿約 8x，最大回撤 406.5%），
跟完全不用槓桿的現貨指數比較，本來就不是同一個風險量級的比較，報酬率高只是承擔了
遠高於指數的風險換來的；這次把風險拉到同一個量級後，TX 轉倉策略反而是落後的一方。

比較有趣的是最大回撤：儘管逐日波動度校準到完全相同（19.41%），TX 策略的最大回撤
（36.9%）反而比 TAIEX（51.4%）低。逐日波動相近不代表最大回撤路徑相近——這可能
跟兩邊報酬的時間相關性（trending vs mean-reverting）、Phase 4 發現的 Basis
收斂效應、以及交易成本的拖累方式不同有關，這裡先如實記錄現象，沒有進一步拆解。

## 結論

1. **風險調整後，直接投資台股加權指數比 TX 無縫轉倉策略報酬率更高**：這是這次
   模擬最主要的發現，也修正了 Phase 3/4 報告裡「TX 報酬率超越大盤」這個說法的
   適用前提——那個結論只在高槓桿、高風險的情境下成立，不是風險調整後的結論。
2. **要讓 TX 期貨部位的波動風險降到跟現貨指數相近，需要用低於 1 倍的槓桿**（本金
   超過部位名目價值），代表 TX 期貨的原始波動性略高於現貨指數本身。
3. **這次比較用的是固定轉倉法、medium 成本情境**；Phase 5 已經證明轉倉方法之間
   差異極小、成本情境的影響遠小於槓桿/風險這個維度，所以這裡沒有重複跑其他組合，
   結論應該對其他轉倉方法/成本情境同樣成立。

## 驗證方式

1. `go build/vet/test ./...` 全過
2. 校準後兩邊年化波動度數字一致（19.41% / 19.41%），確認解方程式的校準邏輯正確
3. `NetPnL` 與本金無關這個前提沿用 Phase 3 報告已經證明過的性質（`internal/engine`
   單元測試裡的追繳事件測試也驗證過 `NetPnL` 不受追繳影響），這裡直接重用、沒有
   重新證明
